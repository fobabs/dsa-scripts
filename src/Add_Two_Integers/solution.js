/**
 * Return the sum of the two integers
 * @param {number} num1
 * @param {number} num2
 * @return {number}
 */

const sum = (num1, num2) => {
  return num1 + num2;
};

// console.log(sum((num1 = 12), (num2 = 5)));
// console.log(sum((num1 = -10), (num2 = 4)));
